class day06 {
  // IDEA: make grid of Point(x, y) based on input max and min border
  // then determine for each point to which point it belongs using manhatten distance
  //  - if belongs --> place in Map / data structure
  //  - if tied, ignore
  // then filter out locs that have points at border coords
  // then select largest from remaining
}
