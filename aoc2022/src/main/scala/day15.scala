/**
 * IDEA:
 * 
 * compute for every point at y=10 (example) the manhattan distance to every other point. If the manhattan distance is
 * smaller than the distance between target point and its beacon, then this point is also in that range. If this point
 * is then not on any known beacon location, it's definitely not a beacon. 
 */